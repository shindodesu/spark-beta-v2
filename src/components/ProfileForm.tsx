import React from 'react';

const ProfileForm: React.FC = () => {
  return (
    <form>
      {/* ここにプロフィール入力フィールドを追加します */}
      <button type="submit">保存</button>
    </form>
  );
};

export default ProfileForm;